import * as types from "./types";

const INIT_STATE = {};

export const authReducer = (state = INIT_STATE, action) => {
  switch (action.type) {
    case types.USER_LOGGED_IN:
      return state
      default :
      return state
  }
};
